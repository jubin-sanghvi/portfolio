# GitHub Personal Access Token for fetching public repository data at build time.
# Scope required: public_repo (read-only)
# Create at: https://github.com/settings/tokens
# In GitHub Actions: add as a repository secret named PORTFOLIO_GITHUB_PAT
PORTFOLIO_GITHUB_PAT=your_token_here
