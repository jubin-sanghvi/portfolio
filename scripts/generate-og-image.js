/**
 * OG image generator placeholder.
 * For v1, the OG image is committed as a static asset at public/images/og-default.png.
 * To regenerate, use a canvas library like @napi-rs/canvas or sharp.
 */
console.log('OG image generation is not automated in v1.');
console.log('Replace public/images/og-default.png manually or add canvas generation here.');
